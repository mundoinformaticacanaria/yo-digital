{
  "name": "yo-digital",
  "version": "0.2.0-reconstruction",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-structure.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
